some notes:
	If a request is done with insufficient authorizations, the server will return 422 Unprocessable Content

API endpoints:
/auth
	/login #
		method: post
		body: loginInfoModel
		response: jwt_string
		response_type: string
	/isvalid #
		parameters:
			auth=token // token to check for validity
		method: get
		response: true/false
		response_type: string

/apply
	/guide #
		method: post
		body: guideApplicationModel
		response: -

	/tour #
		method: post
		body: tourApplicationModel
		response: -

        /individual #
            method: post
            body: tourApplicationModel
            response: -

		/request_changes
			method: post
			body: tourChangeRequestModel
			response: -
	/fair #
		method: post
		body: fairApplicationModel
		response: -

/respond
	/tours #
	    parameters:
	        tid=tour_id // which tour is this for
	        response=accept/deny // what is the response
	    method: post
	    body: auth_token // auth token

	    /changes
            parameters:
                idt=identifier_token // to keep track of the request
                response=response // accept or reject changes

            method: post
            body: -
            response: -

/internal
	/user
		/profile #
			parameters:
				id=id // user bilkent id
			    authToken= jwt token
			method: get
			response: profileModel
			response_type: json

			/update
				method: post
				body: profileUpdateRequestModel
				response: -

		/requests
			method: get
			body: auth_token
			response: TBD ( a list of tour assignments, fair invitations, etc etc)
			response_type: json

			/respond
				parameters:
					rid=request_id // which request is beind responded to
					response=accept/deny // what is the response
				method: get
				body: auth_token
				response: -

		/tours
			method: get
			body: auth_token
			response: TBD
			response_type: json

			/status-update
				parameters:
					tid=tour_id // which tour is this for
					status=status // which status to update to
				method: post
				body: auth_token
				response: -

			/enroll
				parameters:
					tid=tour_id // which tour to enroll for
				method: post
				body: auth_token
				response: -

			/invite
				parameters:
					tid=tour_id // which tour to invite to
					guid=guide_id // who to invite
				method: post
				body: auth_token
				response: -

			/respond
				parameters:
					tid=tour_id // which tour to respond to
					accepted=boolean_here // do you accept or not
				method: post
				body: auth_token
				response: -

	/management
		/people
			method: get
			body: auth_token
			response: a list of guides/advisors etc and work done by them etc etc
			response_type: json

			/applications
				method: get
				body: auth_token
				response: list of people applications
				response_type: json

				/respond
					parameters:
						id=id // id of the applicant
						response=response // accepted or rejected etc
					method: post
					body: auth_token
					response: -

			/invite
				parameters:
					fid=fair_id // which fair to invite to
					id=id // who to invite
				method: post
				body: auth_token
				response: -

			/fire
				parameters:
					id=fireeid // id of the person to fire
				method: post
				body: auth_token
				response: -

		/fairs
			method: get
			body: auth_token
			response: list of fairs
			response_type: json

			/respond
				parameters:
					fid=fair_id // which fair application (all applications are fairs with status applied) to respond to
					response=response // what is the response (accept/deny)
				method: post
				body: auth_token
				response: -


/internal/analytics/ // TODO TODO TODO TODO TODO